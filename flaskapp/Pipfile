[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]

[packages]
requests = "<3,>=2"
msal = "<2,>=0.6.1"
Flask = ">=1,<2"
Werkzeug = ">=1,<2"
Flask-Session = "~=0.3.2"

[requires]
python_version = "3.8"
